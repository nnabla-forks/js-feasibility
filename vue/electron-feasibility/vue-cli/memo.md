# try vue-cli on mac

    brew install n
    sudo n install stable
    sudo npm install -g @vue/cli
    
    vue create NAME
       (select babel, eslint)
       
    cd NAME
    vue add electron-builder

    npm run electron:build -- -mwl
